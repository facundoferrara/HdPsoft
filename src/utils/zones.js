/** Etiquetas y orden de zonas de golpe, usadas por la planilla de resultados. */

export const ZONE_LABELS = { hand: 'Mano', body: 'Cuerpo', head: 'Cabeza', presa: 'Presa' }

// Botones numéricos principales, en orden de valor creciente. 'presa' se muestra aparte
// (mismo valor que 'head' en la config por defecto, pero es una zona distinta).
export const PRIMARY_ZONES = ['hand', 'body', 'head']
